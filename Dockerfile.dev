# Development Dockerfile - runs Vite dev server with hot-reload
FROM node:18-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Copy source files (will be overridden by volume mount in dev mode)
COPY . .

# Expose Vite dev server port
EXPOSE 3002

# Set environment variables for dev server
ENV NODE_ENV=development

# Start Vite dev server
CMD ["npm", "run", "dev"]




