# Install ElevenLabs Client SDK in Frontend

- **Scope:** `jarek-va-ui`.
- **Subtasks:**
  - Add `@elevenlabs/client` dependency to `package.json`.
  - Ensure it is included in lockfile and CI/tests still pass.
  - If needed, add type declarations or ambient types.
