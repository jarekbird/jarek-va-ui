# Environment variables for jarek-va-ui
# Copy this file to .env and update with your actual values

# Domain name for Traefik routing and SSL certificate generation
# This should match your actual domain (e.g., jarekva.com)
DOMAIN_NAME=jarekva.com

# Optional: Custom API base URL for conversation API
# If not set, defaults to /conversations/api
# VITE_API_BASE_URL=/conversations/api
