# jarek-va-ui Environment Variables
# Copy this file to .env and update with your actual values

# =============================================================================
# API Configuration
# =============================================================================

# Base URL for the cursor-runner API (note-taking conversations)
# Defaults to '/conversations/api' (relative path) if not set
# For development, use: http://localhost:3001/conversations/api
VITE_API_BASE_URL=http://localhost:3001/conversations/api

# =============================================================================
# ElevenLabs Agent Configuration
# =============================================================================

# Feature flag to enable/disable ElevenLabs agent functionality
# When set to 'true', agent-specific UI (Agent Conversations, Agent Config) will be visible
# When set to 'false' or not set, all agent UI will be hidden
# Default: false
VITE_ELEVENLABS_AGENT_ENABLED=false

# Base URL for the ElevenLabs agent service API
# Used for fetching signed URLs, registering sessions, and agent configuration
# For local development: http://localhost:3004
# For production: https://your-domain.com (via Traefik routing)
# Default: '' (empty, will use relative paths)
VITE_ELEVENLABS_AGENT_URL=http://localhost:3004

# (Optional) ElevenLabs agent ID for private agents
# Only required if using a private agent that requires authentication
# If not provided, the default public agent will be used
# VITE_ELEVENLABS_AGENT_ID=your-agent-id-here

# (Optional) Public key for ElevenLabs agent authentication
# Only required for certain agent configurations
# VITE_ELEVENLABS_AGENT_PUBLIC=your-public-key-here

# =============================================================================
# Development Notes
# =============================================================================
#
# After modifying this file:
# 1. Copy to .env: cp .env.example .env
# 2. Update values in .env with your actual configuration
# 3. Restart the dev server: npm run dev
#
# Important: Vite only reads environment variables at build time
# Changes to .env require a server restart to take effect
#
# =============================================================================
# Backend Service Environment Variables
# =============================================================================
#
# The following variables are for BACKEND services (cursor-runner, elevenlabs-agent)
# They are NOT used by this frontend application, but are listed here for reference
#
# cursor-runner/.env:
#   ELEVENLABS_AGENT_ENABLED=true
#   REDIS_URL=redis://redis:6379/0
#
# elevenlabs-agent/.env:
#   ELEVENLABS_AGENT_ENABLED=true
#   ELEVENLABS_API_KEY=your-api-key
#   ELEVENLABS_AGENT_ID=your-agent-id
#   WEBHOOK_SECRET=your-webhook-secret
#   CURSOR_RUNNER_URL=http://cursor-runner:3001
#   REDIS_URL=redis://redis:6379/0
#   PORT=3004
#
